<div class="navbar navbar-light bg-light">
  <%= link_to t('admin.nav.header'), admin_tests_path, class: "navbar-brand" %>

  <div class="navbar-text">
    <%= t('application.welcome', email: current_user.email) %> 
    <%= link_to t('application.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>
<%= content_tag :p, "Привет, #{current_user.first_name} #{current_user.last_name}!", class: "alert alert-info" %>
