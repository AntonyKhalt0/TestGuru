<h1><%= @question.test.title %> <%= t('.question')  %></h1>

<p><%= @question.body %></p>

<h2><%= t('.answers') %></h2>

<%= link_to t('.new_answer'), new_admin_question_answer_path(@question) %>

<table class="table">
  <tr>
    <td>Answer</td>
    <td colspan="3">Actions</td>
  </tr>
  <%= render @question.answers %>
</table>

<%= link_to t('.edit_question'), edit_admin_question_path(@question) %><br>
<%= link_to t('.back'), admin_test_path(@question.test) %><br>
