<nav class="navbar navbar-light bg-light">
  <%= link_to "Test Guru", root_path, class: "navbar-brand" %>

  <span class="navbar-text">
   <% if user_signed_in? %>
        <%= t('application.welcome', email: current_user.email) %> 
        <%= link_to t('application.logout'), destroy_user_session_path, method: :delete %>
        <%= content_tag :p, "Привет, #{current_user.first_name} #{current_user.last_name}!", class: "alert alert-primary" %>
      <% else %>
        <%= link_to "Log in", new_session_path(resource_name) %> | 
        <%= link_to "Sign Up", new_registration_path(resource_name) %> 
      <% end %> 
  </span>
</nav>  

