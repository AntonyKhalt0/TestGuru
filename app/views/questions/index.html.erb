<%= form_with model: [@test, @questions], url: test_questions_path do |form| %>
  <h3>Test:</h3> 
  <%= question_header(@test) %>

  <h4>Questions:</h4>
  <table>
    <tr>
      <th>id</th>
      <th>Body</th>
      <th colspan="3">Action</th>
    </tr>
    <%= render @questions %>

    <td><%= link_to "Создать вопрос", new_test_question_path(@test) %></td>
  </table>

<% end %>
